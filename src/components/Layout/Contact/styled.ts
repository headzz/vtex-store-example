import { PINK_JUSSI } from 'configuration/constants'
import styled from 'styled-components'

export const ContactStyled = styled.section`
  background-color: ${PINK_JUSSI};
  padding: 13.6rem 0;
  width: 100%;
`
