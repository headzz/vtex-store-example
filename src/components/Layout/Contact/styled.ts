import { colors } from 'configuration'
import styled from 'styled-components'

export const ContactStyled = styled.section`
  background-color: ${colors.PINK_JUSSI};
  padding: 13.6rem 0;
  width: 100%;
`
